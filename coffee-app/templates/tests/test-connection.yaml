apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coffee-app.fullname" . }}-test-connection"
  labels:
    {{- include "coffee-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coffee-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
