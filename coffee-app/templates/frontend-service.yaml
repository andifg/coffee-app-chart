apiVersion: v1
kind: Service
metadata:
  name: {{ include "coffee-app.frontend.name" . }}
  labels:
    {{- include "coffee-app.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coffee-app.selectorLabels" . | nindent 4 }}
